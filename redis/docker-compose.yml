version: "3.8"

services:
  redis:
    image: redis:latest
    container_name: redis
    restart: unless-stopped
    networks:
      - redis-net
    expose:
      - 6379
    volumes:
      - ${HOME}.docker/cache/redis/data:/data

networks:
  redis-net:
    driver: bridge
